services:
  demo-abg:
    build: .
    image: demo-abg:latest
    restart: unless-stopped
    ports:
      - "3011:3010"
    environment:
      - PORT=3010
    volumes:
      - demo-abg-data:/app/data

volumes:
  demo-abg-data:
