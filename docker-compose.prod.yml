services:
  demo-abg:
    build: .
    image: demo-abg:latest
    restart: unless-stopped
    ports:
      - "3010:3010"
    environment:
      - PORT=3010
    volumes:
      - demo-abg-data:/app/data

volumes:
  demo-abg-data:
